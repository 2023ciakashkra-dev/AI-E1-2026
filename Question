Implement a Python program to solve the 8-puzzle problem using the A* search CO2algorithm. The program should:and(a) Define a class representing the 8-puzzle board.CO3• Include methods to initialize the board with a given initial state, display the current state, and check if a goal state has been reached.• Implement methods to generate possible successor states by moving the blank tile (0 tile) up, down, left, or right.
(b) Implement the A search algorithm to find the optimal solution.*• Use a suitable heuristic function (such as Manhattan distance or misplaced tiles) to evaluate the cost of each state and guide the search towards the goal state efficiently.• Display the sequence of moves required to solve the puzzle from the initial state to the goal state.
Example Initial State:Use the following initial state for testing your program:[1, 2, 3][4, 0, 6][7, 5, 8]Expected Output:• Display each step showing the puzzle board state after each move until the goal state is reached.• Print the sequence of moves (e.g., "Move 1: Move 3 up").
